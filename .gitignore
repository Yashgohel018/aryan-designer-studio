# Dependencies
node_modules/
frontend/node_modules/
backend/node_modules/

# Build output
frontend/dist/
frontend/.vite/

# Environment files
.env
frontend/.env.local
frontend/.env.production.local
backend/.env

# OS
.DS_Store
Thumbs.db

# Logs
npm-debug.log*
*.log

# ── Live product database ──────────────────────────────────────────────────────
# This file is the admin's live product database.
# It is written to by the server at runtime and must NEVER be overwritten by Git.
# On first deploy, the server starts with an empty [] if the file doesn't exist.
backend/data/products.json
